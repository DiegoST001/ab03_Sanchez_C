<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <!-- Barra de búsqueda -->
        <TextBox Name="searchTextBox" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Margin="10"/>
        <Button Name="searchButton" Content="Buscar" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Margin="220,10,0,0" Click="SearchButton_Click"/>

        <!-- DataGrid para mostrar los estudiantes -->
        <DataGrid Name="studentsDataGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,50,10,10"/>
    </Grid>
</Window>
